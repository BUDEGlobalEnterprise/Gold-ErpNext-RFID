export const meta = {
  name: 'zevar-monitor-design-roadmap',
  description: 'Design best-in-class target architecture for the 4 Zevar monitor modules + phased implementation roadmap',
  phases: [
    { title: 'Platform', detail: 'shared cross-cutting platform design (realtime engine, KPI/calc layer, design system, shell)' },
    { title: 'Design', detail: 'per-module target design (live / sales / profit / workforce)' },
    { title: 'Roadmap', detail: 'phased implementation sequencing, milestones, risk, MVPs' },
  ],
}

const PLAN = args.plan_root
const CRIT = args.critical_corrections || []
const CRIT_BLOCK = '\n\nCRITICAL P0 BUGS (evidence-backed; supersede any prior baseline note):\n' + JSON.stringify(CRIT)

const CONTEXT = 'PRODUCT: "Zevar" is a jewelry retail ERP/POS built on the Frappe framework + ERPNext (Python/MariaDB; doctypes under zevar_core/unified_retail_management_system/doctype/*; whitelisted APIs under zevar_core/api/*.py) with a Vue 3 SPA frontend ("zevar_ui") at frontend/zevar_ui/src. Multi-store jewelry retailer: POS sales, gold purchase/scrap, repairs, layaways, memos/consignment, gemstone/metal inventory, appraisals, commissions, customer accounts. GOAL: combine the best features from global jewelry POS leaders into ONE refined superset, implemented module-by-module, to make Zevar the leading jewelry POS. Output must be concrete and engineering-ready (file paths, doctype names, table columns, endpoint names).'

phase('Platform')
log('Designing shared platform (realtime engine, KPI/calc layer, design system, shell, role model)...')
const platform = await agent(
  CONTEXT + '\n\nYou are the CHIEF PLATFORM ARCHITECT. Design the SHARED CROSS-CUTTING PLATFORM all 4 modules build on, so they form ONE coherent world-class monitoring suite rather than 4 silos. This platform unblocks every module.\n\nFIRST, READ THESE FILES for full context (use the Read tool):\n- ' + PLAN + '/00_raw/wf1_consolidated.md  (use: Combined Vision, Differentiation Thesis, Strategic Pillars, Shared Platform Requirements, Quick Wins)\n- ' + PLAN + '/02_current_state_audit/CURRENT_STATE_AUDIT.md  (use: the "Cross-cutting Architecture" section for current-state problems)\n' + CRIT_BLOCK + '\n\nThen produce a complete, concrete platform spec with sections:\n1. REALTIME / EVENT-BUS ENGINE — channels, room/user scoping rules (never broadcast employee events globally), versioned event schema (Python TypedDict + TS mirror), push-first with exponential-backoff polling fallback, 24h rolling backfill log, and the Vue composables (useRealtime, useDashboardData, usePolling) with exact responsibilities.\n2. UNIFIED KPI & CALCULATION LAYER — a sales_monitor.py aggregation service owning ALL sales KPIs; Sale Cost Breakdown as the sole COGS/margin spine; a single shared profit_math module (the margin-is-defined-7-ways bug MUST be solved here: one compute_invoice_margin used by commission.py, reports, analytics, and the what-if simulator); Performance Log as the canonical workforce revenue source; MATERIALIZED ROLLUP TABLES with exact column schemas (daily_store_sales_rollup, employee_period_rollup, metal_rate_history, live_event_log) + refresh triggers on invoice submit/cancel.\n3. DESIGN SYSTEM & CHART PRIMITIVES — choose ECharts vs ApexCharts with rationale; list the 6 reusable chart components; shared formatters (utils/format.js: currency/percent/compact/relative-time); shared status-color constants; reuse existing design tokens (.premium-card, styles/tokens.css).\n4. UNIFIED DASHBOARD SHELL + GLOBAL TIME CONTEXT — one hub (expand ReportsHub), router-view SPA nav (eliminate the window.location.href full-page reload at ReportsHub.vue:245), a global time-range Pinia store consumed by all widgets, collapsing the 4 live screens into one role-aware Command Center.\n5. SINGLE ROLE/PERMISSION MODEL — utils/permissions.js as the sole helper, router meta referencing permission keys, client-side route guards with graceful no-access states, reconciled with backend only_for() role lists.\n6. COMPOSABLE REPORT-BUILDER GRAMMAR — metrics x dimensions x filters with the shared filter grammar (Store/Salesperson/Category/Metal/Gem/Channel/SKU/Customer/Period) and same-type=OR/different-type=AND rule.\n7. FOOTFALL/CONVERSION DATA LAYER — Store Traffic Log doctype + people-counter adapter via hardware_bridge.py (or manual POS staff entry as v1).\n8. CHANNEL + STORE-TARGET DIMENSIONS — sales channel on Sales Invoice; store-level Sales Target doctype (distinct from employee Performance Target).\n9. FRAPPE-STACK LEVERAGE MAP — which native doctypes to USE not rewrite (Number Card, Dashboard Chart, Workspace; Frappe Insights Query/Dashboard/Alert; HRMS Appraisal Cycle/Template/Goal/Employee Performance Feedback/Employee Checkin/Shift/Salary Structure/Salary Slip/Employee Incentive/Additional Salary; ERPNext sales_analytics, gross_profit, sales_person_target_variance).\nBe concrete: propose file paths, doctype/table names, columns, endpoint names, and how each module plugs in. Output as polished, well-structured markdown with headings and tables.',
  { label: 'platform-design', phase: 'Platform', schema: { type: 'object', additionalProperties: false, required: ['title', 'markdown'], properties: { title: { type: 'string' }, markdown: { type: 'string' } } } }
)

const MODULES = [
  { key: 'live_monitor', bic_section: 'Best-in-Class — Live Monitoring', audit_section: 'Live Monitor', label: 'Live Monitor' },
  { key: 'sales_monitor', bic_section: 'Best-in-Class — Sales Monitoring', audit_section: 'Sales Monitor', label: 'Sales Monitor' },
  { key: 'profit_intelligence', bic_section: 'Best-in-Class — Profit Intelligence', audit_section: 'Profit Intelligence', label: 'Profit Intelligence' },
  { key: 'workforce_intelligence', bic_section: 'Best-in-Class — Workforce Intelligence', audit_section: 'Workforce Intelligence', label: 'Workforce Intelligence' },
]

phase('Design')
log('Designing 4 modules in parallel (each reads its audit + best-in-class + shared platform)...')
const designs = await parallel(MODULES.map(m => () => agent(
  CONTEXT + '\n\nYou are the LEAD DESIGNER for the ' + m.label + ' module. Produce a COMPLETE, engineering-ready target design that makes it best-in-class — better than any single competitor — while building on the Zevar Frappe/ERPNext stack and the SHARED PLATFORM below.\n\nFIRST, READ THESE FILES (use the Read tool):\n- ' + PLAN + '/02_current_state_audit/CURRENT_STATE_AUDIT.md  -> focus on the "# Audit: ' + m.audit_section + '" section (current state, gaps, structural issues, tech debt, recommendations). For Profit Intelligence this is the corrected audit including the Margin Math Integrity subsection — treat it as authoritative.\n- ' + PLAN + '/00_raw/wf1_consolidated.md  -> use the "## ' + m.bic_section + '" section as the target feature list.\n- ' + PLAN + '/01_research/research_digest.md  -> OPTIONAL reference for vendor UX/KPI inspiration for this module.\n' + CRIT_BLOCK + '\n\n===== SHARED PLATFORM (build within this; reference its primitives) =====\n' + ((platform && platform.markdown) || '(pending)') + '\n\nProduce the design covering ALL of:\n(A) Module vision & role in the suite.\n(B) Target feature set — grouped, each item mapped to the specific gap/competitor it beats.\n(C) Information architecture — screens, layout, widgets, and ROLE-AWARE variants (owner / manager / associate); include TV/kiosk mode where relevant.\n(D) Data model — new/changed doctypes with key fields, materialized tables, and which EXISTING doctypes to reuse (do not rebuild Frappe/ERPNext).\n(E) API surface — endpoint names + purpose + method.\n(F) Realtime & KPI wiring per the shared platform (channels, rooms, event names, rollup tables read).\n(G) KPIs with PRECISE formulas (e.g. UPT = SUM(qty)/COUNT(invoices)).\n(H) UX patterns worth copying (cite the research).\n(I) Permissions & privacy (esp. associate-data isolation).\n(J) Integration with the OTHER 3 modules + native Frappe stack.\n(K) A phased build WITHIN this module — P0 (must), P1 (should), P2 (nice) — each as concrete, assignable tasks.\n(L) Success metrics / acceptance criteria proving it is best-in-class.\nBe specific and opinionated. Output as polished, well-structured markdown with headings, tables, and a KPI table.',
  { label: 'design:' + m.key, phase: 'Design', schema: { type: 'object', additionalProperties: false, required: ['title', 'markdown'], properties: { title: { type: 'string' }, markdown: { type: 'string' } } } }
)))

const validDesigns = designs.filter(Boolean)

phase('Roadmap')
log('Synthesizing phased roadmap + sequencing + risk + MVPs...')
const roadmap = await agent(
  CONTEXT + '\n\nYou are the DELIVERY ARCHITECT. Produce a PHASED IMPLEMENTATION ROADMAP to build the 4 modules into a market-leading jewelry POS, module-by-module, with the shared platform first.\n\nFIRST, READ (use the Read tool):\n- ' + PLAN + '/00_raw/wf1_consolidated.md  -> use the "## Quick Wins" section.\n' + CRIT_BLOCK + '\n\n===== SHARED PLATFORM DESIGN =====\n' + ((platform && platform.markdown) || '(pending)') + '\n\n===== MODULE DESIGNS =====\n' + validDesigns.map(d => '### ' + d.title + '\n' + d.markdown).join('\n\n---\n\n') + '\n\nProduce:\n1. DEPENDENCY-ORDERED PHASE PLAN — Phase 0 (foundations / shared platform) then Phase 1..4 (modules) then Phase 5 (polish/scale). For each phase: scope, entry criteria, exit criteria / demo, deliverables, effort bands (S/M/L), dependencies.\n2. RECOMMENDED MODULE SEQUENCING with rationale — which module first and why. Weigh leverage: Workforce has a rich backend but zero UI and a payroll-affecting wiring bug; Profit has a strong COGS design but the on_submit hook is unwired (zero output) plus 7 inconsistent margin definitions; Sales has no data layer and needs the platform; Live Monitor needs the realtime engine. Give a clear recommended order and justify.\n3. QUICK-WIN / FAST-VALUE SPRINT (P0, days not weeks) — the concrete first batch (wire the profit on_submit/on_cancel hooks + schedule generator + fix the 4 broken contracts; wire the workforce log_sale_event hook + backfill; resurrect dead realtime channels; add live sales to the command center; compute zero-cost KPIs UPT/run-rate/projected-close; adopt one chart lib; extract format utils; collapse 4 live screens into one command center).\n4. MILESTONES & DEMOABLE CHECKPOINTS (what a stakeholder sees at each gate).\n5. RISK REGISTER with mitigations (API rate-limits/latency, conversion-data dependency, payroll-affecting bugs, data migration/backfill, Frappe-page vs SPA coexistence, multi-store scale, the 7-ways-margin refactor touching commission payouts).\n6. PORTFOLIO SUCCESS METRICS — what proves Zevar became the leader (parity+ with The Edge coaching KPIs, conversion-rate live, true contribution margin from the unified profit_math, payroll-grade commissions flowing to Salary Slip, sub-100ms monitor queries via rollups).\n7. MVP DEFINITION per module — the minimum that is already best-in-class vs the gap-closers that come later.\n8. A SPRINT-BY-SPRINT SUGGESTED TIMELINE (assume a small team; express in relative sprints, not calendar dates).\nBe concrete and sequenced so a team can execute one-by-one. Output as polished markdown with headings, a phase table, a sequencing summary, and a risk table.',
  { label: 'roadmap', phase: 'Roadmap', schema: { type: 'object', additionalProperties: false, required: ['title', 'markdown'], properties: { title: { type: 'string' }, markdown: { type: 'string' } } } }
)

return { platform, designs: validDesigns, roadmap }
